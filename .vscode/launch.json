{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug with LLDB",
      "program": "${workspaceFolder}/target/debug/blockchain",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
